/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { GetAlbumUkService } from './getAlbumUk.service';

describe('Service: GetAlbumUk', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [GetAlbumUkService]
    });
  });

  it('should ...', inject([GetAlbumUkService], (service: GetAlbumUkService) => {
    expect(service).toBeTruthy();
  }));
});
